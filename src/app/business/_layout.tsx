import { Slot } from 'expo-router';
import { Text, View } from 'react-native';

// implement the business layout here, using the auth and profiless
export default function BusinessLayout() {
	return (
		<View>
			<Slot />
		</View>
	);
}
